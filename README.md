# Parallel-Dot-Product-of-Two-Arrays
A parallel programming project that computes the dot product of two arrays by dividing the workload into parts, multiplying corresponding elements concurrently, and combining the partial sums.
